[package]
name = "forge-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "forge-play"
path = "src/main.rs"

[dependencies]
forge-foundation = { path = "../forge-foundation" }
forge-engine-core = { path = "../forge-engine" }
rand = "0.8"
