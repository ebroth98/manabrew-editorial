# Generated by build.rs (forge-cardset-archive). Watching this triggers an
# infinite dev-rebuild loop because every cargo invocation rewrites the
# archive in place if the cardsfolder mtime changed.
resources/cardset.rkyv
